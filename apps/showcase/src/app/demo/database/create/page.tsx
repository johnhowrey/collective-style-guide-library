import { DatabaseCreate } from "@/components/seashell/DatabaseCreate";

export const metadata = { title: "Create database · preview" };

export default function DemoDatabaseCreatePage() {
  return <DatabaseCreate />;
}
