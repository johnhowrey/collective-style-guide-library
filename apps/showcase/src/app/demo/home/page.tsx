import { WorkspaceHome } from "@/components/seashell/WorkspaceHome";

export const metadata = { title: "Workspace · preview" };

export default function DemoHomePage() {
  return <WorkspaceHome />;
}
