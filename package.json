{
  "name": "collective-style-guide-library",
  "private": true,
  "version": "0.0.0",
  "description": "The Collective Style Guide Library — enterprise-ready style guides built on a shared accessible foundation.",
  "scripts": {
    "showcase": "pnpm --filter @collective/showcase dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf dist node_modules/.cache && rm -rf node_modules"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
